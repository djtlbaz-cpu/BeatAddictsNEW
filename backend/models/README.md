# Models

This directory holds trained model weights, versioned checkpoints, and any
training scripts that are specific to model architectures.

Expected layout (example):

models/
  checkpoints/
  weights/
  configs/
